export interface IProducts {
    id: string;              
    name: string;           
    description: string;     
    height: number;       
    length: number;          
    width: number;           
}
